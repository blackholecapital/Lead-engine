FILE: /opt/eila-os/factory-xyz/runtime-c-assets/vendor-source/comms/novu/libs/application-generic/src/services/cache/key-builders/crypto.ts

import { createHash as createHashCrypto } from 'crypto';

export const createHash = (str: string): string => {
  const hash = createHashCrypto('sha256');
  hash.update(str);

  return hash.digest('hex');
};
