FILE: /opt/eila-os/factory-xyz/runtime-c-assets/vendor-source/comms/novu/apps/dashboard/src/components/primitives/form/faceted-filter/components/clear-button.tsx

import { cn } from '../../../../../utils/ui';
import { Button } from '../../../button';
import { STYLES } from '../styles';
import { SizeType } from '../types';

interface ClearButtonProps {
  onClick: () => void;
  size: SizeType;
  label?: string;
  className?: string;
  separatorClassName?: string;
}

export function ClearButton({ onClick, size, label = 'Clear filter', className }: ClearButtonProps) {
  return (
    <Button
      variant="secondary"
      mode="ghost"
      size="2xs"
      onClick={onClick}
      className={cn(STYLES.clearButton, STYLES.size[size].input, className)}
    >
      {label}
    </Button>
  );
}
